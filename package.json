{
  "name": "opencode-asteroids",
  "version": "0.1.0",
  "private": true,
  "description": "Classic Asteroids arcade game implemented with HTML5 Canvas",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html game.js favicon.svg dist/"
  }
}
